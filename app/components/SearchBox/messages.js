/*
 * SearchButton Messages
 *
 * This contains all the text for the SearchButton component.
 */
import { defineMessages } from 'react-intl'

export default defineMessages({
  placeHolder: {
    id: 'app.components.SearchButton.placeHolder',
    defaultMessage: 'Search for Products, Brands and More!'
  }
})
