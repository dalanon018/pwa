/*
 * SectionTitle Messages
 *
 * This contains all the text for the SectionTitle component.
 */
import { defineMessages } from 'react-intl'

export default defineMessages({
  itemsFound: {
    id: 'app.components.SectionTitle.itemsFound',
    defaultMessage: '{itemCount} items found'
  }
})
