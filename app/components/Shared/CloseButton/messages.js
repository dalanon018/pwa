/*
 * CloseButton Messages
 *
 * This contains all the text for the CloseButton component.
 */
import { defineMessages } from 'react-intl'

export default defineMessages({
  header: {
    id: 'app.components.CloseButton.header',
    defaultMessage: 'This is the CloseButton component !'
  }
})
