/*
 * Container Messages
 *
 * This contains all the text for the Container component.
 */
import { defineMessages } from 'react-intl'

export default defineMessages({
  errorHeader: {
    id: 'app.containers.Bucket.errorHeader',
    defaultMessage: 'PAGE OFFLINE'
  }
})
