// import { fromJS } from 'immutable';
// import { makeSelectPrivacyPolicyDomain } from '../selectors';

// const selector = makeSelectPrivacyPolicyDomain();

describe('makeSelectPrivacyPolicyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true)
  })
})
