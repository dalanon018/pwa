// import { fromJS } from 'immutable';
// import { makeSelectProductReviewDomain } from '../selectors';

// const selector = makeSelectProductReviewDomain();

describe('makeSelectProductReviewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true)
  })
})
